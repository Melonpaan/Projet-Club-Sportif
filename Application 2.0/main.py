from GUI.interface import GUIManager

if __name__ == "__main__":
    # Crée une instance de l'application GUIManager
    app = GUIManager()
    
    # Lance la boucle principale de l'application Tkinter
    app.mainloop()
